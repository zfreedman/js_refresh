"use strict";
/**
raises base to a power.
@example
//returns 8
@param {number} base
@param {number} power
pow(2, 3);
@return {number} the base raised to the power.
*/
function pow(base, power) {
  return base ** power;
}
